# Base URL of the backend API (must include /api, matches backend's API_PREFIX)
VITE_API_BASE_URL=http://localhost:5000/api

# Origin of the backend server (no /api suffix) — used only to open the
# Google OAuth redirect flow (window.location, not an axios call)
VITE_SERVER_ORIGIN=http://localhost:5000
